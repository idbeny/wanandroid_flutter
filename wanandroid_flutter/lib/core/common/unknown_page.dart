import 'package:flutter/material.dart';

// 路由跳转未知页面
class YBUnknownPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("404 页面找不到")
    );
  }
}
